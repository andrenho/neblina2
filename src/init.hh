#ifndef NEBLINA_INIT_HH
#define NEBLINA_INIT_HH

class Init {
public:
    Init();
    ~Init();

    Init(const Init&) = delete;
    Init& operator=(const Init&) = delete;
};

#endif //NEBLINA_INIT_HH
